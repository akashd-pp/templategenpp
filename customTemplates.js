/* ---------------------------------------------------------
   Custom templates added via manager.html.
   This file is safe to replace wholesale — manager.html's
   "Export customTemplates.js" button generates a fresh copy
   of this exact format. Keep it alongside templates.js,
   engine.js, app.js and style.css in both the web app and
   the browser extension folders.
--------------------------------------------------------- */
const CUSTOM_TEMPLATES = [];
